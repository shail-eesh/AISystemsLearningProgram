# Sample datasets (synthetic)

Generated by `common/data/make_samples.py` from seed `1729`. Issuers are fictional and prices are simulated — nothing here is
real or licensed market data. The column names mirror the real feeds (NSE bhavcopy,
yfinance OHLCV, EDGAR filing sections) so the loaders you write against these samples
work unchanged on live data.

| file | rows | shape mirrors |
|---|---:|---|
| `ohlcv_sample.csv` | 1300 | yfinance daily OHLCV |
| `nse_bhavcopy_sample.csv` | 1300 | NSE cash-market bhavcopy |
| `orders_sample.csv` | 210 | OMS parent orders + fills (paper) |
| `filings_sample.jsonl` | 45 | EDGAR 10-K/10-Q sections |
